Drop licensed webfont files here if they become available.

Expected brand families:

- `Mistrully`
- `Tan St. Canard`
- `Telegraf`

The current production build uses the provided logo image plus system fallbacks, so launch is not blocked by missing font binaries.
